{
  "name": "pip",
  "version": "0.2.0",
  "private": true,
  "description": "Pip - AI bookkeeping assistant for Xero accounting",
  "workspaces": [
    "packages/*",
    "functions/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:lambda": "pnpm --filter '@pip/lambda-*' run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "deploy:dev": "cd terraform && terraform workspace select dev && terraform apply",
    "chat": "tsx examples/chat.ts",
    "admin": "tsx examples/admin.ts",
    "server": "pnpm --filter @pip/server run dev",
    "server:build": "pnpm --filter @pip/server run build",
    "server:start": "pnpm --filter @pip/server run start"
  },
  "devDependencies": {
    "dotenv": "^17.2.3",
    "tsx": "^4.19.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
